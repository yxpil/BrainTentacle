v0.6.20 更新说明

新增：
- [TUI plain 模式 ANSI 彩色] 行协议 REPL 首次获得完整真彩体验：
  - 启动横幅（紫色双线框 + 青色命令名）
  - prompt 动态显示：`[会话标题] model · sid bit>`（青粗标题 + 深灰模型 + 绿色提示符）
  - 回合分隔线 + 时间戳 + 用户回显（青粗） + 思考中 loading + 耗时尾注 `└─ 3.2s`
  - AI 回复绿色、工具输出黄色、错误红粗
  - 管道/E2E 自动降级纯文本（`is_terminal()` 检测，零破坏现有自动化）

修复：
- plain 模式 handle 返回错误时外层统一打印，handle 内部不再二次输出
- 调试 TUI chat 链路时发现 API key 过期导致 401，TUI 行为正确（错误正确返回 → 显示红粗 "错误：HTTP 401"）

v0.6.19 更新说明

新增：
- [MCP stdio 传输] 除已有的 Streamable HTTP 扫描/手动接入外，新增 stdio（JSON-RPC 2.0 over stdin/stdout）传输层：
  - 兼容 Cursor / Claude Desktop 格式：直接粘贴 `{ "mcpServers": { "name": { "command": "...", "args": [...] } } }` JSON 批量导入
  - Windows 下自动 `cmd /c` 包裹 npx/uvx 等 npm 命令（`.cmd` 后缀对 Rust `Command::new` 不可见）
  - 每个子进程独立 spawn → initialize 握手 → tools 自动并入注册表 → 暂停/继续/移除 → kill 进程
  - 零新增依赖：全部用 tokio（process + sync）、serde_json、serde 已有 crate
- [TUI 审批自动放行] `BIT_TUI` 环境变量标记 TUI 模式，`request_approval()` 检测到后直接放行并记录审计，解决没有 WebView 审批通道导致 120 秒超时拒绝工具调用的问题；全屏 TUI 状态栏显示 `🛡tui-自动` 标识
- [TUI 美化] 消息按角色着色（你:青 AI:绿 tool:黄 ERR:红）、自动 HH:MM 时间戳、回合分隔线、可视化光标、完整行编辑（←→ Home/End Ctrl+A/E Ctrl+W/U Delete/Backspace）、命令历史（↑↓）、Tab 补全斜杠命令、F2 会话列表/F3 工具列表/F4 目标待办侧栏面板、busy 状态提示文字

修复：
- [跨进程状态一致性] 5 处 host ↔ agent-worker 分裂修复：`ensure_session()` 自愈、`engine::is_turn_final_error()` 匹配两种中断错误、`engine::list_approvals/interrupt` 双查合并、`registry::reload_custom_tools()` 每次 list_tools/invoke_tool 调用前热刷新自定义工具
- [Windows 10053 RST] `spawn_401_server` 测试辅助函数先 drain 完整 HTTP 请求头再写响应，解决半关闭 socket 导致的 false failure
- [E2E 稳定性] mock-ai `E2E-CMD-SLEEP` 从 1.5s 改为 1s（pwsh 冷启 0.3-0.6s 叠加 1.5s 超过 `FRONT_WINDOW_MS=2000ms` 触发 busy gate）；`skill(save)` mock 参数从 `description` 改为 `summary` 对齐真实 API

依赖变化：无新增 crate

协议变更：
- McpServer 结构体新增 `transport`（http|stdio）、`command`、`args`、`env` 四个可选字段，全部 `#[serde(default)]` 向后兼容
- 持久化文件 `mcp_servers.json` 旧条目自动视为 `transport: "http"`，新条目按传输方式填充
- stdio 传输 JSON-RPC 2.0 行分隔（`\n`），响应中 `result.isError` 字段与 HTTP 路径一致；调用超时 30s、初始化超时 15s
